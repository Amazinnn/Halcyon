{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for all Focus Desktop windows (v1.2)",
  "windows": ["desktop", "chat", "stats", "music", "pet", "grid-overlay", "logos"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}