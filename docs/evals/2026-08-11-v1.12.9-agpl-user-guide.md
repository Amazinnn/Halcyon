# v1.12.9 AGPL Release and User Guide Eval

Date: 2026-08-11
Requirements: #100, #101
Decision: ADR-0030

## Scope

This checkpoint publishes the user-facing README, four real Focus screenshots,
and the `AGPL-3.0-only` licensing metadata. It deliberately excludes the
intermittent pet-host title-strip and acrylic-corner visual follow-up.

## Required evidence

| Check | Status | Evidence |
| --- | --- | --- |
| Four README images | Pass | `docs/assets/readme/v1.12.9/` contains the copied main, chat/pet, workflow, and rest PNG screenshots; the desktop originals were retained. |
| README image links and alt text | Pass | Four relative PNG links and descriptive alternative text are present in `README.md`. |
| License consistency | Pass | Root `LICENSE` is the GNU AGPL v3 text with the Amazinnn notice; package, Cargo, Settings, README, and notice metadata use `AGPL-3.0-only` / AGPLv3. |
| Focused Settings test | Pass | `apps/desktop npm test -- SettingsPopover.test.ts`: 4 tests passed. |
| Frontend test suite | Pass | `apps/desktop npm test`: 13 files and 49 tests passed. |
| Frontend build | Pass | `apps/desktop npm run build` completed; only Vite's existing bundle-size advisory remains. |
| Rust library tests | Pass | `apps/desktop/src-tauri cargo test --lib`: 175 passed, 0 failed; existing compiler warnings remain. |
| event-schema tests | Pass | `packages/event-schema npm test`: 11 valid and 4 invalid fixtures checked, TypeScript passed. |
| Release rebuild | Pass | `launch-focus.cmd rebuild` completed successfully after Focus was closed. |
| Visual confirmation | Pending | User confirms AGPLv3 appears in Settings and README renders four images after the release rebuild. |

## Release boundary

`v1.12.8` remains the historical MIT tag. No claim is made that AGPL changes
the permissions already granted for that tag.
