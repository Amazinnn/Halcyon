# Focus Desktop 需求原话记录

> 约定：自 2026-08-06 起，用户每次提出的新需求/要求，均以**原话**追加到本表，不改动历史条目。
> 状态取值：已实现 / 本计划实现 / 待复现 / 待定。

| # | 日期 | 原话（用户） | 状态 |
|---|------|--------------|------|
| 1 | 2026-08-06 | 我说把加号放在中间的意思是，我认为图标最多只可以出现大概9个，而且它们都必须处于中央，就是正中央的那两行。然后加号它也默认出现在两行的末尾。这就好比你写字往下写的话，那最后一个字总是出现在行的末尾。当然这里还要考虑到换行。而，但是那图标的区域绝对不可以延伸到屏幕的左右两端，这是不可以的，必须居中，一行最多只有5个。 | 已实现（居中 2 行 × 5 列流式网格、最多 9 个、加号在行尾、不触屏边） |
| 2 | 2026-08-06 | 至于那三个视图。我觉得可以单独弄一个图标，然后点击这个图标可以临时出现一个看板，或者说托盘，就是之前弄的那个。但这个是临时的。然后点击这个就点击托盘上面的那三个视图就可以出现。但是当鼠标移动直至离开那个托盘，那个临时托盘就会消失。所以说那个托盘并不是还像之前那样一直。或者可以停留在界面上的。 | 已实现（视图按钮 + 临时托盘，@mouseleave 自动消失） |
| 3 | 2026-08-06 | 那个托盘没有删除（指向桌面截图：屏幕边缘常驻的 logos 胶囊托盘条仍在显示） | 本计划实现（移除常驻 logos 托盘） |
| 4 | 2026-08-06 | 我希望点击开始专注之后出现的那个屏幕正上方的长条胶囊是浮在对话、音乐、宠物图标等窗口的上面的，但是现在好像是反过来了，这个图层的上下是反过来的。 | 本计划实现（raise_topbar 置顶） |
| 5 | 2026-08-06 | 如果我点开图标打开某个应用，比如记事本，就会触发Agent对话、音乐、宠物图标和统计等的那几个窗口，它们会重新出现窗口上方带有叉叉的浅灰色条条，变回原来的普通浮窗。这时候如果对它们进行移动、隐藏或删除，都会出现卡死和报错，不知道怎么恢复。 | 已验证（卡死复现消失） |
| 6 | 2026-08-06 | 还有，之后我每次提出新的涉及要求，你都要专门以原话记录在一份文档里面！不然你忘掉了，比如说显示三种界面的临时托盘忘记设置成鼠标移开自动消失。 | 已实现（本文件，含托盘自动消失示例） |

> 备注：第 6 条即"需求记录机制"本身；示例中的"临时托盘鼠标移开自动消失"对应第 2 条，已实现。
| 7 | 2026-08-06 | 移动桌面布局时，显现的网格线条最好是越靠近当前操作的框框则亮度越高。这个你好像忘记实现了。 | 已实现（浮窗拖拽 grid-overlay 按距离亮度渐变：近≈亮叶绿，远≈0.06） |
| 8 | 2026-08-06 | 仍然有一些短线条是违背了"越靠近越亮"这个规律的。而且我觉得只有周围两个格子是亮的比较好，而不应该让整个屏幕的网格线都具有亮度，越远的、超过两个格子范围的网格就应该直接不亮。而且不应该以这个窗口吸附之后所在的位置为中心，而应该以当前窗口（尤其是在移动时）悬浮的实际位置为中心。也就是说，不是说它放在哪里之后考虑的是周围的格子，而是它悬浮在哪里，考虑的就是周围有多少，所以说这个距离是需要重新计算的。 | 已实现（亮度中心=实际悬浮位置；仅周围 2 格点亮、更远全灭；每条线单次绘制，消除"短线条"双色伪影） |
| 9 | 2026-08-06 | 能不能做到每个网格的边不是整根同一个颜色，而是允许这条边里面有渐变，我希望从亮到没有的那个渐变长度，现在从两格改成1.5格。懂我意思吗？在完成这个之后，我觉得目前的任务可以告一段落了。也请你维护好所有的开发文档，然后我打算结束本对话并前往下一个对话。 | 已实现（整条网格线沿长度连续渐变：线上每点亮度=该点到窗口实际悬浮位置的距离；1.5 格线性衰减到全透明；文档已维护收尾） |

| 10 | 2026-08-06 | 桌面宠物放到最后吧，然后我觉得先就是没有我说这里的OpenCode Agent它其实只是提供一个架构参考，但是我并不是说真真的直接内置一个Agent。我更倾向于通过以Obsidian中插件claudian的形式来植入一个Codex或者Claude Code的CLI，你懂我意思吧？ | 本计划实现（Claudian 式嵌入真实 Codex CLI；设计稿 M3 仅架构参考，ADR-0007） |
| 11 | 2026-08-06 | 还有，接入的agent支持调用该软件提出的一系列cli，并可调用agent本身具有的skill（也可以在UI上给出用户选项，等等），具有一切agent tui所具有的功能。这一点可参照Paseo这个GitHub项目。 | 本计划实现（focus-cli 白名单+审计 skill + skills 透传 + UI 选项） |
| 12 | 2026-08-06 | 当前的交互存在比较大的问题，具体在于我点击图标层的那个加号，显示出来的临时浮窗里，一行里面的每个字都是竖着排列的，这就不对了。但这并不是重点，重点是比如说我点开某个快捷方式，它那个icon图标的文件并没有显示在图标层里面，而是显示的是由我们开发的这个软件提供的站位图案。而且当我点开这些快捷方式图标的时候，软件可以正常打开，但是比如说我可能双击了比较多次，反正就会触发其他浮窗的卡死，包括前面提到的Agent对话、统计和音乐浮窗就卡死了。 | 已实现（已验收：菜单单行、真实图标、单飞去重、后台线程、release 生产协议） |
| 13 | 2026-08-06 | 我希望它能够有玩能够定制，用户自定义导入，桌宠是动图而不是一堆几何图形；桌宠图标区域可以是1×1、1×2、2×1、2×2，也就是横和纵的长度都可以是1或者2，换句话说可允许拉伸；无论拉伸如何，这个桌宠图案必须是居中的，而且它的边界必须不能超过桌宠图标外面的框框，并且和外面那个框框还必须留有一点点距离，也就是不能刚好卡住；桌宠有个对话按钮，那个对话按钮我觉得可以放在桌宠图案没有覆盖到的区域；OpenAI官方有一个hatch pet skill，实现了精灵图的生成流程，也就是从文字或参考图生成固定状态的透明帧，然后自动裁剪校验并合成精灵图，打包为pet.json和spritesheet.webp，我觉得我们就直接吸收这个产物作为输入就可以了。 | 本计划实现（M1 Pet Pack：吸收 hatch-pet 产物，精灵图播放器 + 文件夹导入 + 校验 + 1×1/1×2/2×1/2×2 拖拽手柄 + 居中留边布局，ADR-0009） |

| 14 | 2026-08-07 | 如果是一乘一的话，宠物图标实在太小。建议统一修改：宠物图样内只放置精灵图，不添加其他按键；对话按键仅在鼠标悬停于宠物图标时触发。另外，原宠物按键的导入功能统一移至设置中。其次，虽然支持1×1、1×2、2×1、2×2大小，但缩放时缺少参考网格。建议长按缩放按钮时显示网格，视觉效果类似移动时的靠近变亮、远离变暗，只是缩放时不移动，以此辅助判断缩放比例。再次，4种大小的宠物图标均无法在桌面上移动，原因不明。最后，精灵图虽有动效，但背景色不契合当前环境。建议只生成精灵像素，不生成背景；或退一步，添加背景色淡化选项，使边缘颜色与当前壁纸环境色衔接，该衔接功能设为可选项。另外，可以外置一个优化过后的Hatch Pet skill方便测试用。 | 本计划实现（v1.7.1：纯精灵图布局+hover 对话按钮、缩放网格预览+冲突回弹、修复拖拽移动、透明背景校验+淡化开关、外置 pet-builder skill，ADR-0010） |
| 15 | 2026-08-07 | 目前我发现了3个问题：第一个问题就是点击那个为“下载“文件夹创建的快捷方式，出现了找不到应用程序的问题。第二个问题是对话窗口、宠物图标等窗口在移动时，映射到网格纸上的中心是偏移的。换句话说，虽然遵循“近亮远暗”的原则，但亮部的中心并不在窗口上。此外，缩放宠物图标时也存在相同的问题，即亮部中心不在图标中心。第三个问题：在提供快捷方式的栏目里，左侧有个按钮，点击后会出现“对话”“音乐”“统计”三个窗口。但现在的交互逻辑是，只有鼠标悬停在该按钮上时，这三个选项才会显示。当我移开鼠标去点击这三个窗口时，悬停状态消失，选项也随之隐藏，导致无法点击。这是一个设计和逻辑上的瑕疵。 | 已实现（已验收：文件夹快捷方式可正常打开；视图按钮改点击式展开；网格亮部中心对齐） |
| 16 | 2026-08-07 | 亮部中心恢复了，但是缩放功能失败了，先前的版本缩放没问题。 | 已实现（已验收：缩放预览与提交恢复正常；overlay 创建时即设 WS_EX_NOACTIVATE，前端处理 pointercancel/lostpointercapture 防句柄卡死） |
| 17 | 2026-08-07 | 每次启动时都会出现窗口堆叠，稍后恢复正常，只有极少概率会出现卡死。我权当其为一个无关紧要的小bug。 | 已知小问题（暂不处理）：启动初期浮窗短暂堆叠后自动归位；极少数启动卡死与 Agent 运行时探测相关，用户接受暂不处理 | 